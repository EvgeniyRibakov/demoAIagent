# 🔧 Проблема с терминалом и решение

## 🚨 Обнаруженная проблема:
В вашем PowerShell терминале есть **кириллические символы "qс"** перед каждой командой. Это мешает выполнению команд.

## ✅ Решения:

### Способ 1: Перезапустите терминал
1. **Полностью закройте** PowerShell/терминал
2. **Откройте новый** PowerShell от имени администратора
3. **Перейдите в папку проекта:**
   ```cmd
   cd C:\Users\fisher\PycharmProjects\AI-agent_with_Cursor
   ```
4. **Попробуйте команды:**
   ```cmd
   node --version
   npm --version
   ```

### Способ 2: Используйте cmd вместо PowerShell
1. **Откройте cmd** (не PowerShell)
2. **Перейдите в папку:**
   ```cmd
   cd C:\Users\fisher\PycharmProjects\AI-agent_with_Cursor
   ```
3. **Проверьте Node.js:**
   ```cmd
   node --version
   npm --version
   ```

### Способ 3: Исправьте кодировку PowerShell
```powershell
# Установите UTF-8 кодировку
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8

# Или попробуйте
chcp 65001
```

### Способ 4: Используйте полные пути
Если Node.js установлен, но не в PATH:
```cmd
"C:\Program Files\nodejs\node.exe" --version
"C:\Program Files\nodejs\npm.cmd" --version
```

## 🔍 Проверка установки Node.js:

### Проверьте файлы:
- `C:\Program Files\nodejs\node.exe`
- `C:\Program Files\nodejs\npm.cmd`

### Если файлы есть, но команды не работают:
1. **Добавьте в PATH:**
   - Win + R → `sysdm.cpl`
   - Advanced → Environment Variables
   - System Variables → Path → Edit
   - Add: `C:\Program Files\nodejs\`

2. **Перезапустите терминал**

## 🎯 После исправления терминала:

```cmd
# Проверка
node --version
npm --version

# Установка зависимостей
npm install

# Сборка проекта
npm run build

# Настройка Google Sheets
npm run setup:google
```

## 🚨 Если Node.js не установлен:

1. **Скачайте с официального сайта:**
   - https://nodejs.org/
   - Выберите LTS версию
   - Скачайте `.msi` файл

2. **Установите:**
   - Запустите `.msi` файл
   - Следуйте инструкциям
   - **ВАЖНО:** Отметьте "Add to PATH" при установке

3. **Перезапустите терминал**

---

**Главная проблема - кириллические символы в терминале. Решите это, и всё заработает!** 🎉
